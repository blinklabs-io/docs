---
title: リリースノート - v0.205.0
description: gOuroboros リリースノート - v0.205.0
sidebar:
  hidden: true
---

## v0.205.0

**タイトル:** 認証、終了処理、デコードを改善

**バージョン:** v0.205.0

v0.205.0 では、メッセージ認証、プロトコルの終了処理、メタデータと台帳データの検証、UTxO-RPC 変換、接続ライフサイクルを改善します。

### 🐛 バグ修正

* 改善 **メッセージ認証:** `MessageAuthenticator` は `StakeAuthority` を必須とし、28 バイトの Blake2b-224 `PoolKeyHash` を導出して処理中に KES を検証します。ゼロの stake を拒否し、KES period のオーバーフローを防ぎ、認証情報がない場合は関連する DMQ パッケージを fail closed にします。

* 修正 **終了メッセージの配信:** コンテキスト対応の配信待機により、`BlockFetch` と `ChainSync` の `Stop` は終了メッセージの配信を最大 250 ミリ秒待機し、配信エラーを返します。

* 保持 **メタデータの重複キー:** ネストしたメタデータマップは重複キーを保持し、外側の auxiliary data のラベルマップは引き続き重複を拒否します。

* 整合 **Byron と SSC のデコード:** Byron の dropped-field デコードと SSC の構造検証が、cardano-ledger と互換性のある wire shape に一致します。

* 強化 **Utxorpc の有理数検証:** Shelley、Mary、Alonzo、Babbage の Utxorpc 検証は、埋め込み有理数の nil 値と範囲外の値を拒否します。

* 更新 **プールメタデータ:** メタデータ URL の上限を Conway より前では 64 バイト、Conway 以降では 128 バイトに設定します。メタデータがない場合も UTxO-RPC で安全に扱い、JSON のキーを小文字にします。

* 維持 **UTXO と UTXOW のエラーコンテキスト:** ネストした UTXO と UTXOW の失敗は、Conway と Dijkstra を含む外側の時代コンテキストを保持し、不明な raw CBOR コンテキストも保持します。

* 整備 **証明書の UTxO-RPC 変換:** 証明書の変換は deposit amounts を `Coin` で保持し、stake-vote delegation には証明書の pool hash を使用します。

* 拡張 **接続ライフサイクル:** `KeepAlive`、`Client`、`Server` に `Start`/`Stop` のライフサイクル制御と簡略化した通知コールバックを追加し、legacy callback の優先順位を維持します。

**リリース:** [v0.205.0](https://github.com/blinklabs-io/gouroboros/releases/tag/v0.205.0)

**比較:** [v0.204.7...v0.205.0](https://github.com/blinklabs-io/gouroboros/compare/v0.204.7...v0.205.0)

---

<!-- doc-holiday-watermark -->
<p align="center">
  <a href="https://doc.holiday">
    <img alt="Doc Holiday logo" src="https://doc.holiday/assets/docs-by-doc-holiday.png" width="200">
  </a>
</p>
<p align="center">Docs authored by <a href="https://doc.holiday">Doc Holiday</a></p>