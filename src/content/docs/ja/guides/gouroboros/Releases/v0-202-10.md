---
title: リリースノート - v0.202.10
description: gOuroboros リリースノート - v0.202.10
sidebar:
  hidden: true
---

## v0.202.10

**タイトル:** トランザクション検証、会計、プロトコル互換性を改善

**バージョン:** v0.202.10

このリリースでは、Byron、Conway、Dijkstra 全体の台帳検証、トランザクションと報酬の会計の一貫性、プロトコルパラメータおよび CBOR の処理を改善しました。

### 💪 改善

* 改善 **Byron 軽量委任の検証:** エポック範囲の両端を含めるようになりました。署名生成では wire encoded certificate index を正確に保持し、正しい light および heavy wire tag を使用し、heavy omega を制約しません。

* 整理 **withdrawal の順序とインデックス:** withdrawal は credential でソートされ、script credential が key credential より先になります。`TxInfo` と redeemer のインデックスは一貫して保たれ、nil 値も安全に処理されます。

* 修正 **CBOR 境界テスト:** 配列ヘッダーの境界テストが 32 ビットと 64 ビットの両方のシステムで正しい結果を返すようになりました。

* 防止 **pool deposit の重複請求:** すでに retired になった pool の登録時には deposit を請求し、同じトランザクション内では台帳 era をまたいでも同じ deposit を複数回請求しません。

* 強化 **無効なトランザクションの依存関係:** トランザクションが無効でも、required script witness と reference input の解決を必須にしました。

* 削除 **Conway の不要な update field:** Conway transaction body は update field 6 を受け付けなくなりました。`ProtocolParameterUpdates` の意味を更新し、null または無効なパラメータ値を拒否して、各パラメータの許容範囲を適用します。

* 条件付け **Dijkstra のネットワーク検証:** withdrawal の wrong network チェックは、phase 2 が有効な場合にのみ実行されます。

* 重複排除 **ソート済みトランザクション入力:** 重複する入力によって redeemer のインデックスがずれなくなりました。

* 拡張 **Dijkstra の一括 withdrawal 検証:** top level と subtransaction の body に対して常に検証し、phase 2 が無効なトランザクションも対象にします。残高不足または未登録アカウントには型付きエラーを返します。

* 保持 **Alonzo datum hash:** `ToPlutusData` が Plutus V3 のデータを処理するときも、Alonzo datum hash を保持します。

* 省略 **無効なトランザクションの delegation 検証:** phase 2 が無効なトランザクションでは delegation チェックを省略し、invalid flag の構造的な拒否は維持します。

* 反映 **Conway の実行コスト:** 最低手数料に Conway の execution unit cost を含め、コストを検証してオーバーフローも確認します。

* 維持 **minimum pool cost:** Mary 以降のプロトコルパラメータ更新で `MinPoolCost` を保持します。

* 拒否 **重複する Conway proposal:** Conway は重複する proposal procedure を拒否し、共通 CBOR 処理で重複メンバーを検出します。

* 置換 **浮動小数点による報酬計算:** 報酬計算で正確な有理数および整数演算を使用し、largest remainder を決定的に割り当てます。

### 推奨ネットワーク互換性 ⚠️

| ネットワーク       | 互換性 |
|--------------------|--------|
| mainnet            | ⛔     |
| preprod-testnet    | ✅     |
| preview-testnet    | ✅     |
| musashi            | ✅     |

**リリース:** [v0.202.10](https://github.com/blinklabs-io/gouroboros/releases/tag/v0.202.10)

**比較:** [v0.202.9...v0.202.10](https://github.com/blinklabs-io/gouroboros/compare/v0.202.9...v0.202.10)

---

<!-- doc-holiday-watermark -->
<p align="center">
  <a href="https://doc.holiday">
    <img alt="Doc Holiday logo" src="https://doc.holiday/assets/docs-by-doc-holiday.png" width="200">
  </a>
</p>
<p align="center">Docs authored by <a href="https://doc.holiday">Doc Holiday</a></p>
