---
title: Release Notes - v0.60.1
description: Dingo Release Notes - v0.60.1
---

## v0.60.1 (June 29, 2026)

**Title:** Accelerate Mithril bootstrap recovery, fix Dijkstra tip nonce handling, and refresh dependencies

**Date:** June 29, 2026

**Version:** v0.60.1

Hi folks! Here’s what we shipped in v0.60.1.

### Recommended Network Compatibility ⚠️

| Network             | Compatible |
|---------------------|------------|
| mainnet             | ⛔         |
| preprod-testnet     | ⛔         |
| preview-testnet     | ✅         |

### ✨ Highlights

* Accelerated **recover Mithril bootstrap downloads through interruptions:** Dingo now resumes Mithril bootstrap and download work more smoothly after transient failures, which helps operators restore sync progress faster.

### 💪 Improvements

* Improved **keep Google Cloud Storage access current:** Dingo now uses a newer Google Cloud Storage client, which keeps cloud snapshot and archive workflows aligned with current dependency support.

* Refined **keep Plutigo integrations current:** Dingo now uses a newer Plutigo version, which keeps packaged integration behavior aligned with recent upstream improvements.

* Updated **keep database access current with newer GORM support:** Dingo now uses a newer GORM version, which keeps database handling aligned with current dependency support.

* Refreshed **keep the nview image current:** Dingo now ships with an updated nview image, which keeps packaged deployment assets aligned with the latest release set.

### 🔧 Fixes

* Corrected **calculate Dijkstra tip nonces accurately:** Dingo now calculates at tip nonces correctly for Dijkstra forging, which keeps block production behavior reliable at the chain tip.

### 🧪 Testing, CI, Docs, and Release Tooling

* Updated **keep build attestations current:** Release and build automation now uses a newer attest action, which keeps artifact verification workflows current.

* Modernized **keep Go setup current in automation:** Release and test workflows now use a newer Go setup action, which keeps automation aligned with current tooling support.

* Refreshed **keep workflow caching current:** Release and test workflows now use a newer cache action, which keeps automation aligned with current caching support.

* Revised **keep the Node type package current in example projects:** Example projects now use a newer `@types/node` package version, which keeps example development dependencies current.

* Adjusted **keep the alternate Node type package example current:** Example projects now align another `@types/node` package update, which keeps example dependency definitions current across templates.

* Updated **keep Vite example dependencies current:** Example projects now use a newer Vite version, which keeps local development and build examples aligned with current tooling.

* Refined **keep the alternate Vite example current:** Example projects now align another Vite update, which keeps example tooling definitions consistent across templates.

* Updated **keep the repository v0.60.0 release record aligned:** The repository release history now keeps the v0.60.0 release notes record in sync, which keeps recent release tracking current.

### 🙏 Thank You

Thank you for trying!